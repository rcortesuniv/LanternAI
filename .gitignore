# .NET
backend/**/bin/
backend/**/obj/
*.user

# Node / frontend (frontend/.gitignore covers the rest; this catches the root case)
node_modules/

# Environment / secrets — never commit real config, only .env.example
.env
.env.local
*.env
!*.env.example

# Editors / OS
.vscode/
.idea/
.DS_Store

# .NET runfile discovery cache (auto-generated, changes every session)
dotnet/runfile-discovery/
